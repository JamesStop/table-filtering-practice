import React, {useState} from 'react';

const useToObject = (url) => {
    
    const [object, setObject] = useState({})
    
    
    
    return (
        object
    );
}

export default useToObject;